from app import db
from app.models.referral import Referral
from app.models.user import User  # Ensure this import matches your project structure
import logging

logger = logging.getLogger("app_logger")


class ReferralRepository:
    pass
